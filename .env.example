# ATXP Connection Token (from `npx atxp whoami` or your ATXP config)
ATXP_CONNECTION_TOKEN=your_connection_token_here

# Port for HTTP mode (default: 3000)
PORT=3000

# Transport mode: "http" or "stdio" (default: stdio)
MCP_TRANSPORT=http
